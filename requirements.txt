# ASRLabs 依赖
# 安装命令:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu130
#
# 其他 PyTorch 索引:
#   CPU:  https://download.pytorch.org/whl/cpu
#   CUDA 12.8: https://download.pytorch.org/whl/cu128

click>=8.0
pyyaml>=6.0
stable-ts>=2.17
faster-whisper>=1.0
qwen-asr>=0.0.1
transformers>=4.52,<5.0
huggingface-hub>=0.34,<1.0
torch>=2.12         # 从 PyTorch 索引安装以获得 CUDA/Vulkan 支持
torchaudio>=2.11     # 从 PyTorch 索引安装
numpy>=1.24
nvidia-cublas-cu12  # CUDA 13 环境需补充 CUDA 12 DLL
soundfile>=0.12
sentencepiece>=0.2
tqdm>=4.0

# ── CTC 对齐器 (optional) ──
# 启用 ctc_align 对齐器:
#   pip install -e .[ctc]
# 需 ffmpeg 在 PATH; 需 C++ 编译环境 (Windows: MSVC Build Tools)
# 默认模型 MMS-300m-1130 为 CC-BY-NC 4.0 (非商用), 商用请换 MIT 模型